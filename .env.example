# docker-compose.prod.yml 用。VPS の設置先にコピーして .env として使う。
# 公開ドメイン(共有 nginx-proxy の VIRTUAL_HOST / acme-companion の LETSENCRYPT_HOST)
APP_HOST=http-anatomy.example.com
# Let's Encrypt 証明書の連絡先メール
LETSENCRYPT_EMAIL=you@example.com
# 使用するイメージタグ。latest か、固定したい場合は GHCR の short-sha タグ
IMAGE_TAG=latest
